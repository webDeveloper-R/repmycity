source 'https://rubygems.org'

gem 'jekyll',            '~> 3.0.0'
gem 'sass',              '~> 3.4.10'

gem 'deep_merge'

gem 'coffee-script',     '~> 2.2.0'
gem 'eco',               '~> 1.0.0'
gem 'uglifier',          '2.1.2'
gem 'nokogiri',          '1.6.6.2'
gem 'rake',              '~> 10.3.2'
gem 'httparty',          '~> 0.13.1'
gem 'execjs'

gem 'html-proofer'

group :deploy do
  gem 'colorize'
  gem 'aws-sdk',         '~> 2'
end

# Note that anything in this group won't be installed in production, so don't blindly require it:
#
# begin
#   require 'pry'
# rescue LoadError => e
# end
group :development do
  gem 'pry',                '~> 0.10.1'
end

#group :deploy do
#  gem 'net-ssh',              '~> 2.8.0', :require => 'net/ssh'
#end
